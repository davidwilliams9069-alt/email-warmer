# Gmail Account Warmer

A web-based tool to warm up new Gmail accounts and improve email deliverability.

## Deployment on Vercel

1. Fork/clone this repository
2. Install Vercel CLI: `npm i -g vercel`
3. Run `vercel` and follow prompts
4. Deploy with `vercel --prod`

## Local Development

```bash
npm install
npm run dev
